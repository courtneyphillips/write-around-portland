<%= flash[:notice] %>

<% @publications.each do |pub| %>

  <h3><%= link_to pub.title, publication_path(pub) %></h3>
  <em><%= pub.release_year %></em>
  $ <%= pub.price %>
<% end %>
<hr>
<div>
  Items in cart: <%= @cart_count %>
</div>
<%= link_to "Checkout", new_order_path %>
